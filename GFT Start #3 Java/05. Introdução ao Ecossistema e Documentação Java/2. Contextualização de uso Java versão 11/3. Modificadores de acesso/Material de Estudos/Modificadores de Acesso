public --> qualquer classe de qualquer pacote poderá acessar o atributo, ou a classe ou o método

protected --> qualquer classe definida no mesmo pacote ou subclasse poderá acessar a classe, método ou atributo

sem modificador --> apenas as classes definidas no mesmo pacote podem ter acesso ao atributo ou método

private --> apenas a própria classe tem acesso ao método ou atributo
